#pragma once// Здесь начинается header guard.  FUNCTOINS_H – любое уникальное имя. Обычно используют имя заголовочного файла в качестве этого идентификатора.
#ifndef FUNCTOINS_H
#define FUNCTOINS_H

// А это уже контент файла .h, где и находятся все предварительные объявления
void zadacha1(int Arr[20]);
void zadacha2(int Arr[20]);
void zadacha3(int Arr[20]);
// Конец header guard
#endif